const express = require('express');
const router = express.Router();

const {} = require('../controllers/adminController');
// const {} = require('../utils/validator/adminValidator');

// router.get('/auth/login', );

module.exports = router;
